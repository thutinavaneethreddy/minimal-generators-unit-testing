{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "second-generator",
  "type": "object",
  "properties": {
    "libName": {
      "type": "string",
      "description": "Library name",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What's the library name?"
    }
  },
  "required": ["libName"]
}
